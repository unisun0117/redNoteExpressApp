【周一】2026.07.13日报：POP发版
【3】POP_日常维护/主数据/待办
● 修改分层标签申请页面的特殊商品标签逻辑
● 特殊商品标签新增/导入行自动填充ALL固定值
● 采购价上限查询：创建人列变更字段
● 非商品类型隐藏存储条件字段
● 商品编码校验仅对新增/编辑行生效，跳过已有回显数据
●  待办处理调试面板-实时显示滚动状态与按钮禁用原因
● 待办处理报错：需要先调用处理再调用反馈
【3】POP_商品上架流程迁移及重构
● 核对查询页面-店仓关系，区域在复制新增时带到新增页面
● 查询页面-店仓关系新增运营区域编码、运营区域字段
● 协助后端查看保存、详情、在线编辑check接口等入参情况
● 采购工作台待办处理-新增处理待办按钮、跳转携带taskId/taskStatus、滚动到底部校验
●  在线编辑校验通过后使用check接口返回数据重新渲染表格
【2】OA审批-销售策略节点
●  售价策略节点匹配放宽至包含STRATEGY字样，策略字段编辑控制优化
● 调试模式下显示保存按钮并绕过表单禁用，方便本地调试
● OA销售策略节点-可订可售为可订时置灰策略规则/策略值/A价策略
【周二】2026.07.14日报
【1】POP_版本迭代：POP_0714版本发版(核对代码、解决冲突)
【2】访客小程序和后台管理
● 小程序分包结构：7个分包 + 主包8个页面的完整清单
● 小程序优化措施 ：towxml精简、main.js优化、冗余文件清理
● 小程序坑点记录：分包root冲突、跨分包组件引用、iconfont路径丢失、decode.json引用残留、跳转路径未更新、uView体积过大
● 访客后台信息管理签到类型：新增车闸签到选项0715（发版沟通）
【2】商品主数据
● 物料类型非商品时小类编码选择数据后不触发接口调用
● 实物图上传后AI自动识别品牌/国际条码【20260720_version】
●  OA只读模式不再调用getTempStorageRule接口（生产问题）
【3】POP_商品上架流程迁移及重构（改bug）
● 到仓商品包装有售价时强制售价策略规则为固定价
● 在线编辑校验失败时新增行也能正确渲染
● 在线编辑校验失败时使用updatedRows保留用户修改内容
●  产地税率-产地字段改为渲染描述文本，隐藏描述列
● 菜品类展示人工成本字段
● 店仓关系导入后加载温层存储规则
● 供应商类型为空时禁用后续字段
【1】OA策略节点
● 清空售价策略规则保存后不再自动赋值
● 售价策略规则为空时不调oaStrategyCheck接口
● 去掉到仓商品包装有售价时强制售价策略规则为固定价的逻辑
【周三】2026.07.15日报
【3】OA策略节点
● OA销售策略节点新增导入策略数据功能
● OA导入策略接口参数改为formData方式传递
● OA导入策略支持CSV格式
● OA导入策略flowNo传POP流程号
● POP页flowNo传popFlowNo，OA导入策略使用flowNo
● OA导入策略文件input清空移到finally确保总是执行
● 去掉固定价时前端默认赋值BCD价逻辑，改为接口返回
【2】POP_商品上架流程迁移及重构（改bug）
● 新增xlsx插件，页面引入从csv换成xlsx
● 导出功能从CSV改为Excel格式
● 产地字段当taxType为0时不显示黄色必填样式
● BomTable必填字段添加黄色背景样式
● 配额相关类型结束日期默认99991231且不可编辑，黄色底管控必填；
● POP页清空有效期联动清空配额日期
【2】商品主数据
● 修复接口返回字段未映射到国际条码导致不渲染的问题
● AI接口返回数据解析不兼容处理、条码值强转为字符串
● 修复AI识别后前端异步引用过期问题（核心问题）
● 非商品类型(Z002/Z003)跳过温层存储条件规则接口调用，防止无效请求
【1.5】OA-AI 智能助手
● 申请调用Dify权限，JS脚本文件样式交互优化
【周四】2026.07.16日报：AI周会
【1.5】AI周会
【2.5】OA策略节点
● OA申请页flowStatus改为动态取值，编辑场景保留后端返回的状态
● OA策略节点：新增状态字段、加载详情时赋值、保存入参使用实际状态
● OA侧上架导出精简为指定字段，POP侧保持全量导出
● 导入数据校验弹窗新增售价策略规则字段，支持中文描述编码映射及strategyRule兼容
● 导出Excel支持列宽配置，OA侧ShelfTable导出列设置合理宽度
● 售价策略规则计算增加前置校验，EAN编码/DC仓层级/销售区域为空时不调接口
【3.5】POP_商品上架流程迁移及重构（修复bug，0716正式上线了）
● 配额：纯供货能力清单类型配额字段置灰不可编辑
● 表格校验请求前剥离err字段，避免前端错误状态污染后端数据
● 供货能力清单没有日期，置灰且取消默认
● 价格因素：必填字段添加黄色样式，新增行设置默认值
● 清空有效期时同步处理价格因素行，开始日期清空、结束日期默认
● 虚拟仓非空时自动验收/自动退货必填且标黄，清空虚拟仓联动清空这两个字段
● 价格因素：税率描述改为span只读展示
● 复制行时剥离id和lineNum字段，避免新行携带旧行的标识数据
● 售价策略规则字段统一，避免增删改查时因字段名不一致导致的数据丢失问题
● 排查处理换渠道清空策略值仅限用户手动切换，避免初始化时误清
【0.5】20260716版本，代码核对，构建生产，配置页面
【周五】2026.07.17日报：💴
【1.5】商品主数据
● 收货图+实物图反面，复用实物图正面的AI识别建议弹窗
【1.5】商品主数据
● 封装附件批量上传弹窗组件/附件批量删除弹窗组件
【3】主数据：批量上传弹窗（BatchUploadAttachment）：
● 第一步：9个附件字段选择（收货图、实物图正面/反面、新品过会凭证、外观对比鉴别方法图例、商品检测报告、标签检测报告、供应商资质文件、特殊宣称佐证材料）
● 第二步：两栏布局 — 左侧上传区+待分配文件列表，右侧附件分配明细表
● 共用附件模式开关（开启后全部自动分配）
● 支持文件拖拽到明细行、行间拖拽移动附件
● 上传状态流：- → 等待上传 → 上传中 → 上传成功/失败
【1】POP_日常优化
● 全局生效，所有 <el-table> 表头文字可以原生选中
● 协助查看生产环境-商品主档查询-报错问题
【1】OA-AI 智能助手
● 申请调用Dify权限，JS脚本文件样式交互优化
其他
● AI写周报
周报
一、项目开发与交付
POP

二、需求评审与拆解
● POP_商品上架流程迁移及重构（已上线，后续跟进优化）
● POP_20260720版本发版、其他版本开发

三、协作与支持/其他说明（暂无）

四、个人不足与改进（暂无）

五、下周重点任务
1. 商品主数据：新增附件批量上传/删除功能功能（开发，提测）
2. 商品上架：图片获取、列表搜索新增权限校验等（评审，开发）
3. AI识别商品图片及BOM接口（评审，开发）