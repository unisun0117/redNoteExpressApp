<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.qdm.tob.modules.system.dict.mapper.SysDictItemMapper">

    <select id="search" resultType="cn.qdm.tob.modules.system.dict.domain.SysDictItem">
        SELECT * FROM sys_dict_item
        WHERE dict_code = #{code}
        <if test="keyword != null and keyword != ''">
            AND (`value` LIKE CONCAT('%', #{keyword}, '%') OR `label` LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="status != null and status != ''">
            AND `status` = #{status}
        </if>
        ORDER BY `sort` ASC
    </select>

</mapper>
