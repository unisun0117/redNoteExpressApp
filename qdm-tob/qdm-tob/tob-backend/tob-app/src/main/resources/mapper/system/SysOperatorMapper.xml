<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.qdm.tob.modules.system.operator.mapper.SysOperatorMapper">

    <select id="pageSearch" resultType="cn.qdm.tob.modules.system.operator.domain.SysOperator">
        SELECT * FROM sys_operator
        <where>
            <if test="employeeCode != null and employeeCode != ''">
                AND employee_code LIKE CONCAT('%', #{employeeCode}, '%')
            </if>
            <if test="realName != null and realName != ''">
                AND real_name LIKE CONCAT('%', #{realName}, '%')
            </if>
            <if test="mobile != null and mobile != ''">
                AND mobile LIKE CONCAT('%', #{mobile}, '%')
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="type != null and type != ''">
                AND type = #{type}
            </if>
        </where>
        ORDER BY created_at DESC
    </select>

</mapper>
